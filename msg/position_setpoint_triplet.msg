# Global position setpoint triplet in WGS84 coordinates.
# This are the three next waypoints (or just the next two or one).

px4/position_setpoint previous # 이전 setpoint
px4/position_setpoint current  # 현재 setpoint 
px4/position_setpoint next     # 다음 setpoint
